'use strict';
// The server this build is wired to. VIVATLAS fills this in when you download the
// extension from Settings → Browser extension, so there's no "enter server" step.
// Left blank in the source checkout (the popup then asks for the address once).
window.VIVATLAS_SERVER = '';
